{{- $svc := index .Values.global "auth-service" -}}
apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
stringData:
{{- toYaml $svc.secrets | nindent 2 }}
